# v0.7.0 Validation Matrix

| Field | Value |
|------|-----|
| Version | 0.7.0 |
| Status | DRAFT |
| Created | 2026-05-17 |

## Risk Pack × Verification Command Matrix

| Risk Pack | Verification Command | Covered Workstream | Associated Rule |
|-----------|---------|-----------------|----------|
| runtime-streaming | `make test-nginx-unit-streaming` | A | 1, 2, 38 |
| runtime-streaming | `make verify-chunked-native-e2e-smoke` | A | 1, 2, 38 |
| runtime-streaming | `make verify-streaming-failure-cache-e2e` | A, E | 1, 2, 38 |
| ffi-boundary | `make test-rust` | A, B | 15 |
| ffi-boundary | `make test-rust-fuzz-smoke` | A, B | 15 |
| ffi-boundary | `make build && make check-headers` | A | 15 |
| ffi-boundary | `make test-nginx-unit` | A, B | 15 |
| nginx-protocol-safety | `make test-nginx-unit` | A | Baseline |
| nginx-protocol-safety | `make test-nginx-integration` | A, B | Baseline |
| nginx-protocol-safety | `make test-e2e-rust` | A, B | Baseline |
| observability-metrics | `make test-nginx-unit` | A, B, E | 7, 8, 23 |
| observability-metrics | `make release-gates-check` | B, E | 7, 8, 23 |
| packaging-distribution | `make docs-check` | C, D | 9 |
| packaging-distribution | `dpkg-deb --info` / `rpm -qip` | C | 9 |
| packaging-distribution | `helm lint charts/nginx-markdown` | D | 9 |
| dynamic-config-hot-reload | `make test-nginx-unit` | E | 34, 35 |
| dynamic-config-hot-reload | `make verify-streaming-failure-cache-e2e` | E | 34, 35 |
| docs-tooling-drift | `make docs-check` | C, D, E | 9 |
| docs-tooling-drift | `make release-gates-check-strict` | E | 9 |
| release-governance | `make release-gates-check` | C, E | 13 |
| harness-remediation | `make harness-check` | All | 36 |
| harness-remediation | `make harness-check-full` | All | 36 |

## Workstream × Verification Command Matrix

| Workstream | Minimum Verification Set | Full Verification Set |
|-----------|-----------|-----------|
| A | `make test-nginx-unit-streaming && make test-rust && make verify-chunked-native-e2e-smoke` | + `make test-rust-fuzz-smoke && make test-nginx-integration && make test-e2e-rust && make coverage-c && make coverage-rust` |
| B | `make test-rust && make test-nginx-unit && make coverage-rust` | + `make test-rust-fuzz-smoke && make test-e2e-rust && make coverage-c` |
| C | `make docs-check && make release-gates-check` | + DEB/RPM smoke, signature verification |
| D | `make docs-check && helm lint charts/nginx-markdown` | + K8s smoke, E2E |
| E | `make test-nginx-unit && make docs-check && make harness-check` | + `make verify-streaming-failure-cache-e2e && make release-gates-check-strict && make harness-check-full` |

## Gate × Verification Command Matrix

| Gate | Verification Command | Blocking Condition |
|------|---------|---------|
| Gate 1 (P0 Correctness) | `make test-nginx-unit-streaming && make test-rust && make test-rust-fuzz-smoke && make verify-chunked-native-e2e-smoke && make coverage-c && make coverage-rust` | Any command non-zero exit |
| Gate 2 (Rust-first) | `make test-rust && make build && make check-headers && make test-e2e-rust` | Any command non-zero exit |
| Gate 3 (Package) | DEB/RPM build passes + signature verification + smoke tests | Build failure or invalid signature |
| Gate 4 (K8s) | `helm lint` + K8s smoke | helm lint fails or smoke does not pass |
| Gate 5 (Operability) | `make harness-check-full && make docs-check && make release-gates-check-strict` | Any command non-zero exit |

## Document Updates

| Version | Date | Author | Changes |
|------|------|------|------|
| 0.7.0-draft | 2026-05-17 | spec-agent | Initial v0.7.0 validation matrix |
