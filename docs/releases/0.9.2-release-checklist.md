# 0.9.2 Release Checklist

**Current status:** The 0.9.2 candidate (rc5, HEAD `91ce38c9`) has its
remote workflow set passing on the release head, with the remaining
checklist items bound to a candidate SHA. The public contract, runtime
cleanup, package checks, and release documentation now match. Release
artifacts and publication remain pending their own evidence.

## Pre-Release

- [ ] All changes for 0.9.2 merged to `main` / release branch
- [x] CHANGELOG.md updated with `[0.9.2]` section
- [x] `docs/releases/0.9.2-release-notes.md` written
- [x] `docs/guides/MIGRATION-0.9.2.md` written
- [x] `docs/guides/UPGRADE-TO-0.9.2.md` written
- [x] `docs/guides/VERSION_ROLLBACK-0.9.2.md` written
- [x] Version references updated in source, chart, and docs (Rule 55)
- [x] THIRD-PARTY-NOTICES synchronized with Cargo.lock (Rule 49)

## Build and Test

- [x] `make test-rust` passes
- [x] `make test-nginx-unit` passes
- [x] `make test-rust-fuzz-smoke` passes
- [x] `make complexity-check` passes (Rule 17)
- [x] `make security-static` passes (Rule 48)
- [x] `make docs-check` passes
- [x] `make harness-check` passes
- [x] `make harness-security-checks` passes
- [x] `make check-headers` passes
- [x] `make test-e2e-rust` passes
- [x] `make test-package-compatibility` passes
- [x] `make release-gates-check` passes (current feature-oriented contract)
- [ ] `make release-gates-check-092` passes
- [ ] `perf/baselines/module-baseline-092.json` and its retained raw/probe
      evidence pass the provenance-bound blocking validator
- [x] `make coverage-c` meets threshold (Rule 25)
- [x] `make coverage-rust` meets threshold (Rule 25)

## Release Gates 0.9.2

- [ ] `make release-gates-check-092-canonical` passes the current 0.9.2
      contract and compatibility regression checks. The gate uses baseline 091
      only as a provenance-bound performance comparison input. It is not a second
      release-gate chain.
- [ ] `make release-gates-check-092` passes the full candidate-bound 0.9.2
      release gate after the canonical subset
- [ ] Tag `release-gate` runs the same candidate-bound blocking evidence once.
      The `publish` job requires that job to succeed for tags
- [ ] `make perf-evidence-check` passes (non-blocking)

The retired generic 0.5.0 validator and the 0.9.0/0.9.1 wrapper chain are not
release prerequisites. The focused 0.7.0/0.8.0 validators remain only as
compatibility regression checks owned by the current 0.9.2 gate.

## Packaging

- [ ] nFPM-generated DEB package builds and installs cleanly
- [ ] RPM package builds and installs cleanly
- [x] `packaging/nfpm/nfpm.yaml` and its maintainer scripts match the 0.9.2
      package contract
- [x] `packaging/rpm/SPECS/nginx-module-markdown.spec` has 0.9.2 %changelog entry
- [x] Module `.so` filename is canonical across all packaging formats (Rule 13)
- [ ] `check_install_layout.sh` passes for nFPM DEB and RPM packages

## Smoke Tests

- [ ] Prebuilt module loads on NGINX 1.24.0
- [ ] Prebuilt module loads on NGINX 1.26.3
- [ ] Prebuilt module loads on NGINX 1.28.3
- [ ] Prebuilt module loads on NGINX 1.30.4
- [ ] Prebuilt module loads on NGINX 1.31.4
- [ ] `nginx -t` passes with 0.9.2 module and a migrated 25-directive config,
      0.9.1 configs using removed directives fail with `unknown directive`
      (breaking-freeze contract, see `docs/guides/0.9.2-breaking-changes.md`)
- [ ] Diagnostics endpoint returns version 0.9.2
- [ ] `recent_decisions[].reason` can carry `bypass_no_transform`
- [ ] The module rejects Diagnostics POST with `action=rollback` (read-only contract)
- [ ] The watcher picks up atomic restore of a valid dynconf file
- [ ] Invalid PID sources fail the dynconf E2E, only a completely absent PID
      source produces the polling SKIP
- [x] Prometheus metrics render correctly
- [x] Doctor tool passes

## Retired 0.9.2 Surfaces

- [x] Standalone streaming decision-state implementation and its dead tests
      are absent. The runtime retains only the phase/terminal latches needed
      for request lifetime and backpressure.
- [x] OTel, profile, per-path metrics, shadow-mode, and reject-only migration
      surfaces remain historical documentation only and are not active code.
- [x] Retired release validators and duplicate E2E wrappers are absent, with
      their still-required invariants covered by the current feature-oriented
      gates.

## Documentation

- [x] All new directives/features documented in `docs/guides/CONFIGURATION.md`
- [x] Release notes cross-reference migration and upgrade guides
- [x] AGENTS.md rule index updated if new rules added
- [x] `docs/harness/routing-manifest.json` updated for new gates
- [x] The current branch head passes the complete required GitHub workflow set

## Post-Release

- [ ] Git tag `v0.9.2` created and pushed
- [ ] GitHub Release created with artifacts and checksums
- [ ] Helm chart published with updated appVersion
- [ ] Docker image published and tagged
- [ ] Homebrew formula updated (if applicable)
- [ ] Release checklist archived

## Verification Record

Local verification on 2026-08-27 records the following evidence before
publication:

- Rust and native module unit suites, Rust fuzz smoke, complexity, static
  security, headers, Rust E2E, package compatibility, current release gates,
  harness checks, and blocking harness-security checks passed.
- `make coverage-gate` passed the native NGINX E2E and C/Rust coverage
  thresholds, including streaming backpressure and truncated-codec cases.
- Package maintainer-script and removal-lifecycle suites passed. DEB/RPM
  artifact build/install checks still require the Linux x86_64 artifact run.
- Linux x86_64 native GCC C units and all package-compatibility suites passed.
  The Linux node could not pull the pinned Ubuntu Docker image because Docker
  Hub timed out. The Docker-specific parity target remains unchecked.
- The current branch head (rc5 `91ce38c9`) passed the required local
  harness and unit suites in the latest verification round. the qualification run
  records the remote workflow set for the exact candidate head when the
  candidate-bound run completes. Previous rc1-era runs no longer apply (
  do not cite them for the rc5 head).
- Candidate-bound 0.9.2 gates, provenance-bound baseline evidence, release
  artifacts, and publication remain unchecked until their own evidence is
  available.

## Document Updates

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 0.9.2 | 2026-08-27 | Codex | Synchronized candidate validation status and publication prerequisites |
| 0.9.2 | 2026-08-27 | Codex | Synchronized current release gates, retired surfaces, package verification, local evidence, and exact-SHA publication prerequisites |
| 0.9.2 | 2026-07-30 | Kang | Initial release checklist for 0.9.2 |
