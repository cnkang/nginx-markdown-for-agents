# mod-markdown-for-agents.conf
#
# This file is an OPTIONAL reference snippet for the nginx-markdown-for-agents
# dynamic module. It is NOT automatically loaded or enabled by the package
# installer.
#
# This file is installed to /usr/share/nginx/modules-available/ as an inactive
# reference only. NGINX will NOT read this file unless you explicitly copy,
# symlink, or include it in your configuration.
#
# To enable the module, you must MANUALLY add the load_module directive to your
# nginx.conf (top-level, before the http block). You can either:
#
#   1. Copy the directive below directly into nginx.conf, or
#   2. Use your own include mechanism if your NGINX setup supports it.
#
# After making changes:
#   sudo nginx -t
#   sudo systemctl reload nginx
#

# Uncomment the following line to load the module:
#load_module modules/ngx_http_markdown_module.so;
